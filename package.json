{
  "name": "@nophira/nova",
  "private": true,
  "license": "MIT",
  "author": "Nophira",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "build:nova-init": "cd apps/nova-init && npm run build",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "check": "turbo run check"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "ts-node": "^10.9.2",
    "turbo": "^2.5.5",
    "typescript": "^5.9.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.5.2",
    "express": "^5.1.0"
  }
}
